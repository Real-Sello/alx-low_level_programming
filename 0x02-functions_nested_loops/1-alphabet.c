#include "main.h"

/**
 * main - prints lowercase alphabet
 * Return: always 0 (success)
 */

void print_alphabet(void)
{
	int alph;
	for (alph = 'a' ; alph <= 'z' ; alph++)
	{
		_putchar('alph');
		_putchar('\n');
	}
	return (0);
}
