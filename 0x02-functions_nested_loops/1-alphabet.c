#include "main.h"

/**
 * main - prints lowercase alphabet
 * Return: always 0 (success)
 */

void print_alphabet(void)
{
	int a;
	for (a = 'a' ; a <= 'z' ; alph++)
	{
		_putchar('a');
		_putchar('\n');
	}
}
